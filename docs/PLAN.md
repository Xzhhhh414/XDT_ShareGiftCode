# 开发计划

本文档记录《心动小镇》兑换码聚合工具的开发计划。Phase 1 静态 HTML MVP 已完成，后续逐步引入后端存储、审核和采集能力。

## 产品规则调整

### 文档同步规则

修改任何产品行为、界面文案、交互流程、数据结构、样式布局、规则判断或开发阶段计划时，必须同步更新产品文档。

当前产品文档入口：

- `README.md`：记录当前产品定位、核心规则、页面结构和 MVP 范围。
- `docs/PLAN.md`：记录阶段计划、已完成进度、后续演进和需要保留的产品决策。

例外情况：

- 纯格式化
- 注释调整
- 不改变用户可见行为的内部整理

使用例外时，需要在交付说明中明确说明无需更新产品文档的原因。

### 状态精简

前台不再在兑换码卡片顶部展示 `可用` / `待验证` 状态标签。

展示规则：

- 有明确有效期且尚未过期的兑换码，直接视为当前可用。
- 无法确认有效期的兑换码，展示缺失明确有效期和最新玩家反馈，并允许玩家反馈 `可用` 或 `失效`。
- “已失效 / 已过期”的兑换码不再展示给玩家，但通过玩家反馈判定失效的未知有效期兑换码会保留 12 小时缓冲。

说明：

- “即将过期”属于有效期提示，不作为独立状态。
- 内部数据可以保留更细状态，用于审核、采集、隐藏和排序。

### 有效期文案统一

前台有效期只使用以下文案格式：

```text
今日 24:00 前
7月31日 24:00 前
7月7日 20:00 前
```

规则：

- 来源写“今日有效”“当天 24 点前”等，统一展示为 `今日 24:00 前`。
- 来源给出日期但没有具体时间，统一展示为 `M月D日 24:00 前`。
- 来源给出具体时间，统一展示为 `M月D日 HH:mm 前`。
- 没有解析到有效期，不展示 `有效期：有效期不确定`，改为展示缺失明确有效期和最新玩家反馈。
- 已过期兑换码不进入玩家可见列表。

### 过期码展示规则

确定过期的兑换码不展示给玩家。

隐藏条件：

- `expireAt < now`
- 来源明确表达“已失效”“不能用了”“过期了”
- 未知有效期兑换码的最新玩家反馈为 `失效`，且反馈时间已经超过 12 小时

隐藏后的数据仍可以保留在后台或数据文件里，用于：

- 避免重复采集
- 避免重复提交
- 追溯来源
- 训练或优化有效期解析规则

### 玩家反馈规则

对于无法通过来源确认有效期或有效性的兑换码，允许玩家反馈：

- `可用`
- `失效`

反馈不应由单个玩家直接决定全局状态。

前台展示规则：

- 不展示可用 / 失效累计数量。
- 有明确有效期的兑换码，不展示玩家兑换成功或失败。
- 只在无明确有效期的兑换码上展示最新一次玩家反馈结果和时间。
- 无明确有效期的兑换码，卡片内展示 `分享使用结果` 按钮。
- 点击 `分享使用结果` 后打开弹窗，弹窗内用感谢文案说明玩家反馈价值，并由玩家选择 `可用` 或 `失效`。
- 同一张卡片同时存在奖励补充和使用结果反馈时，两个按钮在卡片底部并排展示。
- 最新反馈为 `可用` 时，展示 `缺失明确有效期，MM/DD HH:mm 玩家兑换成功`。
- 最新反馈为 `失效` 时，展示 `缺失明确有效期，MM/DD HH:mm 玩家兑换失败，已失效`。
- 通过玩家反馈判定失效的未知有效期兑换码，不会立刻下架；最新失效反馈超过 12 小时后隐藏。

## 是否需要服务器

静态 MVP 可以不需要服务器，但只能验证页面体验。

如果要让工具真实服务玩家，就需要服务器或等价的后端存储，因为以下信息需要跨玩家共享：

- 兑换码列表
- 玩家提交的新兑换码
- 可用 / 失效反馈
- 玩家补充的奖励内容
- 复制次数
- 来源采集结果
- 人工审核结果
- 过期隐藏判断结果

当前本地 `localStorage` 只能记录单个浏览器的反馈和奖励补充，不具备真实运营价值。

## 阶段计划

### Phase 1：静态 MVP（已完成）

目标：完成可体验的前端工具，验证布局、信息结构和玩家操作流程。

范围：

- 静态 HTML / CSS / JS
- PC / Mobile 响应式布局
- 静态数据文件
- 兑换码卡片展示语义标题，不直接用兑换码本身作为标题
- 兑换码卡片展示礼包码发布日期，并使用显眼标签突出
- 卡片顶部不展示状态标签
- 有效期文案统一
- 过期码前台隐藏
- 默认按发布时间倒序展示
- PC 大屏使用三列兑换码卡片，窄桌面和平板使用双列，Mobile 使用单列卡片
- 一键复制
- 本地反馈交互，未知有效期兑换码通过弹窗发布使用结果
- 本地奖励补充交互，奖励待确认的兑换码通过弹窗填写奖励内容
- 头图下方展示兑换码使用方法说明

不做：

- 后端存储
- 自动采集
- 用户登录
- 真实跨用户反馈统计
- 来源筛选
- 兑换码数量统计模块
- 最新来源面板
- 玩家反馈统计面板
- 提交兑换码入口

### Phase 2：轻量后端与共享数据

目标：让兑换码、提交和反馈成为真实共享数据。

范围：

- 增加后端 API
- 存储兑换码列表
- 存储玩家反馈
- 存储玩家补充的奖励内容
- 存储玩家提交的新兑换码
- 前端从接口读取数据
- 提交和反馈写入接口
- 后台或管理方式支持人工审核
- 根据最新玩家反馈同步前台展示和隐藏判断

建议数据表：

```text
gift_codes
code
title
game
status
expire_at
expire_text
reward
visible
first_seen_at
last_seen_at
created_at
updated_at

gift_code_sources
code_id
platform
source_url
source_title
source_excerpt
found_at

gift_code_feedback
code_id
result
created_at
client_id

gift_code_reward_feedback
code_id
reward
review_status
created_at
client_id

gift_code_submissions
code
source_url
note
review_status
created_at
```

### Phase 3：采集与语义解析

目标：降低人工维护成本，半自动发现新兑换码。

范围：

- TapTap 公开来源采集
- 小红书人工提交链接优先，后续再评估采集方式
- 抽取疑似兑换码
- 解析兑换码附近上下文
- 生成玩家可读的兑换码标题
- 解析有效期
- 识别“可用 / 失效”语义
- 合并同码多来源
- 进入人工审核队列

解析策略：

- 先用规则识别明确日期和时间
- 再用语义模型处理“今日有效”“当天 24 点前”“过期了”等自然语言
- 标题优先从来源上下文、奖励内容、有效期语义生成；信息不足时使用兜底标题，不直接拿兑换码本身当标题
- 不确定时不要强行给过期时间，前台展示缺失明确有效期和最新玩家反馈

### Phase 4：排序信号与自动隐藏

目标：让列表排序和隐藏更接近真实可用情况。

范围：

- 根据信号调整排序，不对玩家展示百分比评分
- 根据最新玩家反馈调整展示和隐藏
- 根据过期时间自动隐藏
- 根据失效反馈阈值隐藏
- 保留隐藏记录用于去重和追溯

排序和隐藏信号：

- 来源数量
- 来源平台
- 来源发布时间
- 最近发现时间
- 最新玩家反馈结果
- 最新玩家反馈时间
- 是否解析到明确有效期

## 当前进度

Phase 1 已完成并收口，当前没有需要继续补齐的前端 MVP 缺口。已完成内容：

- 兑换码卡片已增加语义标题，兑换码本身不再作为卡片标题。
- 兑换码卡片已增加礼包码发布日期标签，优先使用 `firstSeenAt`，只显示月日。
- 语义标题和兑换码文本已做视觉区分，兑换码与复制按钮保持轻量尺寸，字重低于标题层级。
- 复制按钮需跟随兑换码文本使用紧凑尺寸，避免抢标题层级。
- 奖励为 `奖励待确认` 的兑换码已增加 `分享奖励内容` 入口，弹窗内支持玩家填写奖励内容；静态 MVP 阶段先保存在浏览器本地。
- 兑换码卡片顶部状态标签已移除。
- 有效期展示已统一。
- 前台列表会过滤已过期兑换码。
- 筛选已移除，默认按发布时间倒序展示。
- PC 兑换码列表已调整为大屏三列、窄桌面和平板双列，Mobile 保持单列。
- 复制按钮已调整到兑换码右侧，并缩小按钮尺寸。
- `可用` / `新增` / `待验证` 数量模块已移除。
- 最新来源面板和玩家反馈统计面板已移除。
- 提交兑换码入口已移除。
- 兑换码使用方法说明已移动到头图下方。
- 玩家反馈入口只在无明确有效期的兑换码上展示为 `分享使用结果`，并通过弹窗选择 `可用` 或 `失效`。
- 前台只在无明确有效期的兑换码上展示最新一次玩家反馈结果和时间，不展示可用 / 失效累计数量。
- 通过玩家反馈判定失效的未知有效期兑换码，会在最新失效反馈超过 12 小时后隐藏。

下一步进入 Phase 2 前，需要先确定后端存储、审核流程和共享反馈方案。
